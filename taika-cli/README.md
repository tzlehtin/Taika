# taika-cli

The native Linux terminal dashboard and human-in-the-loop interaction layer.

## Core Responsibilities
* Built with a stable version of `spring-shell-starter` (3.3.1).
* Connects directly to `taika-swarm` to trigger, monitor, and configure active optimization loops.
* Provides native interactive terminal commands (e.g., `forge swarm --run`).