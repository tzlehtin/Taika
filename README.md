# Taika
Taika — A lightweight, open-source AI swarm orchestrator built with Spring Boot for Linux. It forces LLMs into a strict, deterministic compilation and error-correction loop, leveraging Maven compiler and test feedback to create working code.
